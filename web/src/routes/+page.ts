// One prerendered page. The example is chosen client-side, so there is nothing
// per-example to generate and a shared link still answers 200 from a static
// host rather than the 404 an SPA fallback would give.
export const prerender = true
